# NaviMovie-Maker v0.2.0

## 主な変更

### プレイリストの保存と再利用

変換キュー、URL／ローカルファイル、処理設定、プレイリスト固有の出力フォルダを `.nmm-playlist.json` として保存・再読込できるようになりました。プレイリストごとに異なる出力先を維持できます。

### 標準的な保存操作

新規作成（`Ctrl+N`）、開く（`Ctrl+O`）、保存（`Ctrl+S`）、名前を付けて保存（`Ctrl+Shift+S`）に対応しました。初回保存後は同じファイルへ上書きし、未保存の変更がある状態で新規作成・別ファイルを開く・終了する場合は確認します。

### mpvによるプレイリスト再生

`Ctrl+P` でキュー順のプレイリストを mpv へ渡して再生できます。すべての再生可能項目に検証済み結果がある場合は処理結果を、未処理項目がある場合は元のローカルファイル／URLを再生します。URL の対応範囲は mpv と yt-dlp に依存します。

mpv は標準 ZIP には同梱されません。「ツール」→「設定」→「外部ツール」で別途インストールした `mpv.exe` を選択してください。

### 処理結果の追跡

成功したダウンロード、変換、コピーの結果をプレイリストへ保存します。「結果」列で出力済み、出力なし、出力変更あり、要再処理などを確認でき、有効な既存結果は再実行時に処理を省略します。

### 出力ファイルの連番同期

連番付きプレイリストを並べ替えた後、追跡済みの有効な結果ファイルについて、先頭の連番だけを再変換せずに同期できます。実行前に変更内容を確認できます。

### 出力名の衝突回避

所有者不明の既存ファイルを上書きせず、ソース識別子に基づく安定した名前を使って衝突を回避します。

## 互換性

旧形式 v1 のプレイリストを読み込めます。旧ファイルにない項目 ID は読込時に生成され、追跡情報のない既存ファイルを自動的にプレイリストの結果とは判断しません。

## 動作環境

- Windows x64
- .NET 10 Desktop Runtime (Windows Desktop Runtime x64)
- URL取得・変換には yt-dlp、FFmpeg、ffprobe
- プレイリスト再生には別途 mpv

## 注意事項

- 本アプリはコード署名されていないため、Windows SmartScreen や Smart App Control の警告が表示される場合があります。
- 動画サイトの利用規約、著作権法、権利者の許諾条件を遵守してください。
- URL の取得・再生、変換後ファイルの再生可否は、外部ツール、サイト、再生機器の仕様に依存します。
