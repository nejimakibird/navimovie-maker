# NaviMovie-Maker v0.2.1

v0.2.1 では、ReplayGain を利用した音量正規化を追加し、通常の変換結果へ不要な識別子が付く出力ファイル名の問題を改善しました。v0.2.0 のプレイリスト保存、処理結果管理、mpv 再生などの機能は引き続き利用できます。

## 主な変更

### 音量の正規化（dB）

- FFmpeg の ReplayGain 解析を利用
- XMedia Recode に近い 89.0 dB 基準の目標音量指定
- 80.0～105.0 dB の範囲を 0.1 刻みで設定可能
- ピーク上限を超える場合はリミッターで保護
- 従来のピーク音量方式と選択可能
- 通常モードと Simple Mode の両方に対応
- ReplayGain 解析に失敗した場合は、正規化なしで変換を継続

### 出力ファイル名の改善

- 衝突がない通常時は `動画タイトル.mp4` のような識別子なしの名前を使用
- プレイリスト連番が有効な場合は `001_動画タイトル.mp4` のように先頭連番だけを追加
- 管理外ファイルや別ソースの成果物と実際に衝突した場合だけ、安定した識別子を追加
- 既存ファイルを上書きしない安全な方針を維持
- 過去の識別子付き追跡済み成果物との互換性を維持

## 動作環境

- Windows x64
- .NET 10 Desktop Runtime
- FFmpeg、ffprobe、yt-dlp、mpv などの外部ツールは標準 ZIP に非同梱

## ダウンロード

`NaviMovie-Maker-v0.2.1-win-x64.zip`

GitHub Releases の自動生成される `Source code` ではなく、上記のアプリ本体 ZIP をダウンロードしてください。

## 注意事項

- 本アプリはコード署名されていないため、Windows SmartScreen や Smart App Control の警告が表示される場合があります。
- 動画や音声の保存・変換・再生では、著作権法、権利者の許諾条件、各サービスの利用規約を遵守してください。
- XMedia Recode と FFmpeg では解析方法や入力条件が異なるため、音量解析結果が完全には一致しない場合があります。
